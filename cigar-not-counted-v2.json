{
  "name": "Cigar Not Counted v2",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 4 * * 0"
            }
          ]
        }
      },
      "id": "c2000000-0000-4000-8000-000000000001",
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [0, 0]
    },
    {
      "parameters": {
        "url": "https://abbiescigars.phppointofsale.com/index.php/api/v1/reports/inventory_not_scanned_during_count?start_date=2026-06-01&end_date=2026-09-30&limit=5000&offset=0",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {}
      },
      "id": "c2000000-0000-4000-8000-000000000002",
      "name": "Fetch Not Scanned",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [220, 0],
      "executeOnce": true
    },
    {
      "parameters": {
        "url": "https://abbiescigars.phppointofsale.com/index.php/api/v1/items?category_id=2&limit=2000",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {}
      },
      "id": "c2000000-0000-4000-8000-000000000003",
      "name": "Fetch Cigar Items",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [440, 0],
      "executeOnce": true
    },
    {
      "parameters": {
        "jsCode": "// Cigars category is id 2 (hardcoded, saves an API call).\n// Both HTTP nodes are executeOnce so each endpoint is hit exactly one time.\nconst report = $('Fetch Not Scanned').all().map(i => i.json);\nconst cigarItems = $('Fetch Cigar Items').all().map(i => i.json);\n\nconst cigarNumbers = new Set();\nconst cigarNames = new Set();\nfor (const it of cigarItems) {\n  if (it.item_inactive === true) continue;\n  if (it.item_number) cigarNumbers.add(String(it.item_number).trim().toLowerCase());\n  if (it.item_id) cigarNumbers.add(String(it.item_id).trim().toLowerCase());\n  if (it.name) cigarNames.add(String(it.name).trim().toLowerCase());\n}\n\nlet rows = [];\nfor (const r of report) {\n  if (r && r.data && Array.isArray(r.data.rows)) rows = rows.concat(r.data.rows);\n  else if (r && Array.isArray(r.details)) rows = rows.concat(r.details);\n  else if (r && r.data && Array.isArray(r.data)) rows = rows.concat(r.data);\n}\n\nfunction pick(obj, keys) {\n  for (const k of keys) {\n    for (const actual of Object.keys(obj)) {\n      if (actual.toLowerCase().replace(/[^a-z]/g,'') === k) return obj[actual];\n    }\n  }\n  return '';\n}\n\nfunction csvCell(v) {\n  const s = String(v === null || v === undefined ? '' : v);\n  return /[\",\\n]/.test(s) ? '\"' + s.replace(/\"/g, '\"\"') + '\"' : s;\n}\n\nconst out = ['sku,name,on_hand'];\nlet matched = 0;\nconst totalRows = rows.length;\n\nfor (const row of rows) {\n  const num = String(pick(row, ['itemnumber','sku','item','number','itemid']) || '').trim().toLowerCase();\n  const nm  = String(pick(row, ['name','itemname','description']) || '').trim().toLowerCase();\n  if (!((num && cigarNumbers.has(num)) || (nm && cigarNames.has(nm)))) continue;\n  matched++;\n  out.push([\n    csvCell(pick(row, ['itemnumber','sku','item','number','itemid'])),\n    csvCell(pick(row, ['name','itemname','description'])),\n    csvCell(pick(row, ['quantity','onhand','instock','qty']))\n  ].join(','));\n}\n\nreturn [{\n  json: {\n    file_path: 'business-decisions-data/cigar-not-counted.csv',\n    total_report_rows: totalRows,\n    cigar_rows: matched,\n    report_columns: rows.length ? Object.keys(rows[0]).join(' | ') : 'NO ROWS',\n    debug_first_row: rows.length ? JSON.stringify(rows[0]) : 'NO ROWS',\n    commit_message: 'Cigar not-counted list (' + matched + ' cigars of ' + totalRows + ' report rows)',\n    file_content: out.join('\\n') + '\\n'\n  }\n}];"
      },
      "id": "c2000000-0000-4000-8000-000000000004",
      "name": "Build CSV",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [660, 0]
    },
    {
      "parameters": {
        "resource": "file",
        "operation": "create",
        "owner": { "__rl": true, "value": "1JKT1", "mode": "name" },
        "repository": { "__rl": true, "value": "abbies-data", "mode": "name" },
        "filePath": "={{ $json.file_path }}",
        "fileContent": "={{ $json.file_content }}",
        "commitMessage": "={{ $json.commit_message }}"
      },
      "id": "c2000000-0000-4000-8000-000000000005",
      "name": "Create File",
      "type": "n8n-nodes-base.github",
      "typeVersion": 1,
      "position": [880, 0],
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "resource": "file",
        "operation": "edit",
        "owner": { "__rl": true, "value": "1JKT1", "mode": "name" },
        "repository": { "__rl": true, "value": "abbies-data", "mode": "name" },
        "filePath": "={{ $('Build CSV').all()[0].json.file_path }}",
        "fileContent": "={{ $('Build CSV').all()[0].json.file_content }}",
        "commitMessage": "={{ $('Build CSV').all()[0].json.commit_message }} [rerun]"
      },
      "id": "c2000000-0000-4000-8000-000000000006",
      "name": "Update File",
      "type": "n8n-nodes-base.github",
      "typeVersion": 1,
      "position": [1100, 100]
    }
  ],
  "connections": {
    "Schedule Trigger": { "main": [[{ "node": "Fetch Not Scanned", "type": "main", "index": 0 }]] },
    "Fetch Not Scanned": { "main": [[{ "node": "Fetch Cigar Items", "type": "main", "index": 0 }]] },
    "Fetch Cigar Items": { "main": [[{ "node": "Build CSV", "type": "main", "index": 0 }]] },
    "Build CSV": { "main": [[{ "node": "Create File", "type": "main", "index": 0 }]] },
    "Create File": { "main": [[], [{ "node": "Update File", "type": "main", "index": 0 }]] }
  },
  "settings": { "executionOrder": "v1", "timezone": "America/New_York" }
}
